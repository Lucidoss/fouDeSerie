{% extends 'base.html.twig' %}


{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	<div class="example-wrapper">
		<h1>Les series de Fou de Séries ?</h1>
		<h1>Il y a
			{{ nombreSeries }}
			series omggg </h1>
			<ul>
				{% for series in lesSeries %}
					<h2>{{ series.titre }}</h2>
					<a href="{{path('app_detail', {'id' : series.id}) }}"><img src="{{series.image}}" style="width:90vh"></a>
                    <button><a href="{{path('app_admin_deleteSerie', {'id':series.id , 'token':csrf_token('delete_serie')})}}">SUPPRIMER LA SERIE</a></button>
                    <form method="post" action="{{path('app_admin_deleteSerie', {'id':series.id})}}">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="token" value="{{csrf_token('delete_serie')}}">
                    <button>Supprimer</button>
                    </form>
				{% endfor %}
			</ul>
		</div>
	{% endblock %}
