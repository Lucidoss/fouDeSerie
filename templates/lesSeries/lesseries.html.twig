{% extends 'base.html.twig' %}
{% block stylesheets %}
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Sharp:opsz,wght,FILL,GRAD@48,400,0,0" />
{% endblock %}
{% block javascript %}
<script src="/js/like.js"></script>
{% endblock %}
{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	<div class="example-wrapper">
		<h1>Les series de Fou de Séries ?</h1>
		<h1>Il y a
			{{ nombreSeries }}
			series omggg </h1>
			<ul>
				{% for series in lesSeries %}
					<h2>{{ series.titre }}</h2>
					<a href="{{path('app_detail', {'id' : series.id}) }}"><img src="{{series.image}}" style="width:90vh"></a>
					<a href="{{path('app_serieslikes', {'id' : series.id}) }}" id="likes">{{series.likes}} likes <span class="material-symbols-sharp">
thumb_up
</span></a>
				{% endfor %}
			</ul>
		</div>
	{% endblock %}
