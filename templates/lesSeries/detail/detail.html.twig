{% extends 'base.html.twig' %}


{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	<div class="example-wrapper">
			<ul>
					<h2>Titre<br>{{ laSerie.titre }}</h2>
					<p>Resumé<br>{{ laSerie.resume }}</p>
					<p>Premiere diffusion<br>{{ laSerie.premiereDiffusion | date("m/d/Y") }}</p>
					<img src="{{laSerie.image}}" style="width:90vh">
			</ul>
		</div>
	{% endblock %}
